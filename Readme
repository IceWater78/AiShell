# AiShell 智能运维终端

## 功能

- **多标签终端**：SSH / Telnet / Rlogin / 串口 / SFTP（文件会话）五协议；SSH 支持密码 / 私钥 / SSH Agent（Pageant 自动回退）/ 键盘交互认证，旧设备 SSH 算法兼容开关（老款交换机 KEX/CBC/DSS）
- **多设备并发与同设备多连接**：≥20 台设备同时保持连接（单设备最多 8 条、全局 64 条上限保护）；同一设备可开多条连接独立标签（#2、#3…），聊天记录 / 模式 / 事实按设备共享
- **广播输入**：同步键入到多台已选终端
- **分屏**：窗格二叉树 + 每窗格标签栈（拖拽拆分、四向分屏、分隔条调比例，≤6 窗格）
- **ZMODEM 传输（rz/sz）**：终端流内上传/下载，文件名编码跟随终端编码
- **SFTP 文件管理**：双栏面板（本地只读浏览 + 远端 CRUD/上传/下载
- **连接级终端编码**：点击「编码」切换 UTF-8 / GBK / GB18030等编码
- **会话管理器**：使用独立json保存会话信息
- **会话日志**：逐连接记录终端输出，位置可指定，断开自动收尾
- **AI 运维 Agent**：右侧自然语言对话，Agent操作当前标签页的设备；所有命令回显在终端里，所见即所发生；连接断开时挂起的确认卡立即终止并提示
- **Agent 管控（Harness 配置）**：菜单「工具 ( T ) → AI 管控 ( H )」——执行控制（默认模式 / 步数 / 单轮配额 / 危险级逐字确认）、权限策略（四级处置 / 黑白名单正则）
- **事实库**：菜单「工具 ( T ) → 事实库 ( B )」——按会话查看设备事实
- **安全 Harness**：四级风险分类 + 四种执行模式 + 意图范围校验 + 全量审计
- **厂商画像**：华为 VRP / 华三 Comware / 思科 IOS / 通用，连接后自动识别，含各自命令风格提示与危险命令规则
- **终端个性化**：配色方案（内置 + 自定义 + 导入导出）、字体（预置 + 字体文件导入）、字号、鼠标行为可配
